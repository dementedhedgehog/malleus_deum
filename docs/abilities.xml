<?xml version="1.0" encoding="utf-8"?>
<!--

   Just the abilities table.

   
  -->
<book>
  <chapter>
    <chaptertitle>Abilities</chaptertitle>
    
    {% from "docs/macro_ability.xml" import build_ability with context %}    
    <section>
      <sectiontitle>Lore Abilities</sectiontitle>
      {% for ability in ability_groups.get_abilities()
      if ability.is_lore_ability() and not ability.is_gmg_ability() %}
      {{ build_ability(ability) }}    
      {% endfor %}      
    </section>

    <section>
      <sectiontitle>Martial Abilities</sectiontitle>
      {% for ability in ability_groups.get_abilities()
      if ability.is_martial_ability() and not ability.is_gmg_ability() %}
      {{ build_ability(ability) }}    
      {% endfor %}      
    </section>

    <section>
      <sectiontitle>General Abilities</sectiontitle>
      {% for ability in ability_groups.get_abilities()
      if ability.is_general_ability() and not ability.is_gmg_ability() %}
      {{ build_ability(ability) }}    
      {% endfor %}      
    </section>

    <section>
      <sectiontitle>Magic Abilities</sectiontitle>
      {% for ability in ability_groups.get_abilities()
      if ability.is_magic_ability() and not ability.is_gmg_ability() %}
      {{ build_ability(ability) }}    
      {% endfor %}      
    </section>
  </chapter>  
</book>
